# API → Database → UI Flow Diagram

Visual representation of the complete data flow.

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER LOGIN FLOW                              │
└─────────────────────────────────────────────────────────────────┘

1. USER ACTION
   ┌──────────────┐
   │ Login Form   │ User enters email/password
   │ (UI)         │ Clicks "Login" button
   └──────┬───────┘
          │
          ▼
2. REMIX ACTION
   ┌─────────────────────────────┐
   │ apps/frontend/app/routes/   │
   │ login.tsx                    │
   │ action() function           │
   └──────┬──────────────────────┘
          │ Extracts formData
          │ Calls loginUser()
          ▼
3. AUTH SERVER UTILITY
   ┌─────────────────────────────┐
   │ apps/frontend/app/lib/      │
   │ auth.server.ts              │
   │ loginUser()                 │
   └──────┬──────────────────────┘
          │ Makes API call
          ▼
4. API CLIENT
   ┌─────────────────────────────┐
   │ apps/frontend/app/lib/      │
   │ api.ts                       │
   │ api.post("/auth/login")      │
   └──────┬──────────────────────┘
          │ HTTP POST Request
          │ Body: { email, password }
          │ No Authorization header (public)
          ▼
5. BACKEND CONTROLLER
   ┌─────────────────────────────┐
   │ apps/backend/src/auth/      │
   │ auth.controller.ts          │
   │ @Post("login")              │
   └──────┬──────────────────────┘
          │ Calls authService.login()
          ▼
6. AUTH SERVICE
   ┌─────────────────────────────┐
   │ apps/backend/src/auth/      │
   │ auth.service.ts             │
   │ validateUser()              │
   └──────┬──────────────────────┘
          │ Queries database
          ▼
7. DATABASE QUERY
   ┌─────────────────────────────┐
   │ Prisma Client               │
   │ prisma.user.findUnique()    │
   └──────┬──────────────────────┘
          │ SQL: SELECT * FROM User WHERE email = ?
          ▼
8. DATABASE
   ┌─────────────────────────────┐
   │ PostgreSQL                  │
   │ Returns user record         │
   └──────┬──────────────────────┘
          │ User data returned
          ▼
9. PASSWORD VALIDATION
   ┌─────────────────────────────┐
   │ bcrypt.compare()             │
   │ Compares password hash      │
   └──────┬──────────────────────┘
          │ Password valid?
          ▼
10. TOKEN GENERATION
    ┌─────────────────────────────┐
    │ JWT Service                │
    │ jwtService.sign()          │
    └──────┬──────────────────────┘
           │ Generates accessToken & refreshToken
           │ Payload: { email, sub, role, organizationId }
           ▼
11. RESPONSE
    ┌─────────────────────────────┐
    │ AuthResponseDto             │
    │ { accessToken, refreshToken,│
    │   user: {...} }             │
    └──────┬──────────────────────┘
           │ JSON response
           ▼
12. API CLIENT RECEIVES
    ┌─────────────────────────────┐
    │ api.ts                      │
    │ Returns AuthResponse        │
    └──────┬──────────────────────┘
           │ Response data
           ▼
13. SESSION STORAGE
    ┌─────────────────────────────┐
    │ session.server.ts           │
    │ setAuthTokensInSession()    │
    └──────┬──────────────────────┘
           │ Stores tokens & user in session
           │ Creates cookie
           ▼
14. REDIRECT
    ┌─────────────────────────────┐
    │ login.tsx action()          │
    │ redirect("/dashboard")      │
    └──────┬──────────────────────┘
           │ Sets Set-Cookie header
           ▼
15. BROWSER
    ┌─────────────────────────────┐
    │ Cookie: __session           │
    │ Contains: tokens & user     │
    └─────────────────────────────┘
```

---

## 🛡️ Protected Route Flow (Dashboard)

```
┌─────────────────────────────────────────────────────────────────┐
│              DASHBOARD DATA LOADING FLOW                        │
└─────────────────────────────────────────────────────────────────┘

1. USER NAVIGATION
   ┌──────────────┐
   │ Browser       │ User navigates to /dashboard
   │ URL Change    │
   └──────┬───────┘
          │
          ▼
2. REMIX LOADER
   ┌─────────────────────────────┐
   │ apps/frontend/app/routes/   │
   │ _protected.dashboard.tsx    │
   │ loader() function           │
   └──────┬──────────────────────┘
          │ Calls requireAuth()
          ▼
3. AUTH CHECK
   ┌─────────────────────────────┐
   │ auth.server.ts               │
   │ requireAuth()                │
   └──────┬──────────────────────┘
          │ Gets session from cookie
          │ Extracts user & token
          │ If no user → redirect to /login
          ▼
4. SESSION EXTRACTION
   ┌─────────────────────────────┐
   │ session.server.ts            │
   │ getSessionFromRequest()      │
   └──────┬──────────────────────┘
          │ session.get("user")
          │ session.get("accessToken")
          ▼
5. FETCH DATA
   ┌─────────────────────────────┐
   │ dashboard.server.ts          │
   │ fetchEventsData(token)       │
   └──────┬──────────────────────┘
          │ Calls API with token
          ▼
6. API CLIENT
   ┌─────────────────────────────┐
   │ api.ts                       │
   │ api.get("/events")           │
   └──────┬──────────────────────┘
          │ HTTP GET Request
          │ Headers: {
          │   Authorization: "Bearer <token>"
          │ }
          ▼
7. BACKEND CONTROLLER
   ┌─────────────────────────────┐
   │ events.controller.ts         │
   │ @Get() findAll()             │
   └──────┬──────────────────────┘
          │ @UseGuards(JwtAuthGuard)
          ▼
8. JWT AUTH GUARD
   ┌─────────────────────────────┐
   │ jwt-auth.guard.ts            │
   │ canActivate()                │
   └──────┬──────────────────────┘
          │ Extracts token from header
          │ Validates token
          ▼
9. JWT STRATEGY
   ┌─────────────────────────────┐
   │ jwt.strategy.ts             │
   │ validate(payload)           │
   └──────┬──────────────────────┘
          │ Decodes JWT token
          │ Gets user ID from payload
          │ Queries database for user
          ▼
10. DATABASE QUERY (User)
    ┌─────────────────────────────┐
    │ Prisma Client               │
    │ prisma.user.findUnique()    │
    └──────┬──────────────────────┘
           │ SQL: SELECT * FROM User WHERE id = ?
           ▼
11. DATABASE
    ┌─────────────────────────────┐
    │ PostgreSQL                  │
    │ Returns user record         │
    └──────┬──────────────────────┘
           │ User data
           ▼
12. GUARD SETS req.user
    ┌─────────────────────────────┐
    │ jwt.strategy.ts             │
    │ return user                 │
    └──────┬──────────────────────┘
           │ req.user = user
           ▼
13. EVENTS SERVICE
    ┌─────────────────────────────┐
    │ events.service.ts           │
    │ findAll(filters)            │
    └──────┬──────────────────────┘
           │ Builds WHERE clause
           │ Filters by organizationId
           │ Applies role-based filtering
           ▼
14. DATABASE QUERY (Events)
    ┌─────────────────────────────┐
    │ Prisma Client               │
    │ prisma.event.findMany()     │
    └──────┬──────────────────────┘
           │ SQL: SELECT * FROM Event 
           │      WHERE organizationId = ?
           │      ORDER BY createdAt DESC
           ▼
15. DATABASE
    ┌─────────────────────────────┐
    │ PostgreSQL                  │
    │ Returns event records       │
    └──────┬──────────────────────┘
           │ Events array
           ▼
16. SERVICE RETURNS
    ┌─────────────────────────────┐
    │ events.service.ts           │
    │ return events               │
    └──────┬──────────────────────┘
           │ Events data
           ▼
17. CONTROLLER RETURNS
    ┌─────────────────────────────┐
    │ events.controller.ts        │
    │ return events               │
    └──────┬──────────────────────┘
           │ JSON response
           │ Status: 200
           ▼
18. API CLIENT RECEIVES
    ┌─────────────────────────────┐
    │ api.ts                       │
    │ Returns events array         │
    └──────┬──────────────────────┘
           │ Response data
           ▼
19. LOADER PROCESSES
    ┌─────────────────────────────┐
    │ dashboard.server.ts          │
    │ calculateStats()            │
    │ fetchBudgetData()            │
    │ fetchAlerts()               │
    └──────┬──────────────────────┘
           │ Transforms data
           │ Calculates statistics
           ▼
20. LOADER RETURNS
    ┌─────────────────────────────┐
    │ _protected.dashboard.tsx    │
    │ return json({...})          │
    └──────┬──────────────────────┘
           │ LoaderData object
           ▼
21. COMPONENT RENDERS
    ┌─────────────────────────────┐
    │ DashboardRoute component    │
    │ useLoaderData()             │
    └──────┬──────────────────────┘
           │ Receives loaderData
           │ Passes props to <Dashboard>
           ▼
22. UI DISPLAYS
    ┌─────────────────────────────┐
    │ Dashboard component         │
    │ Renders events, stats, etc. │
    └─────────────────────────────┘
```

---

## 🔄 Complete Request Cycle

```
┌─────────────────────────────────────────────────────────────────┐
│                    REQUEST CYCLE                                 │
└─────────────────────────────────────────────────────────────────┘

CLIENT SIDE (Frontend)
──────────────────────
┌──────────────┐
│ UI Component │
│ (React)      │
└──────┬───────┘
       │ User interaction
       ▼
┌──────────────┐
│ Remix Route  │
│ loader()     │
│ action()     │
└──────┬───────┘
       │ Server-side execution
       ▼
┌──────────────┐
│ API Client   │
│ api.get()    │
│ api.post()   │
└──────┬───────┘
       │ HTTP Request
       │ Headers: Authorization: Bearer <token>
       │ Body: JSON data
       │
       │═══════════════════════════════════════════════│
       │              NETWORK BOUNDARY                 │
       │═══════════════════════════════════════════════│
       │
       ▼
SERVER SIDE (Backend)
──────────────────────
┌──────────────┐
│ NestJS       │
│ Controller   │
└──────┬───────┘
       │ Route handler
       ▼
┌──────────────┐
│ Auth Guard   │
│ JWT Strategy │
└──────┬───────┘
       │ Validates token
       │ Sets req.user
       ▼
┌──────────────┐
│ Service      │
│ Business     │
│ Logic        │
└──────┬───────┘
       │ Processes request
       │ Builds query
       ▼
┌──────────────┐
│ Prisma       │
│ Service      │
└──────┬───────┘
       │ Prisma query
       │
       │═══════════════════════════════════════════════│
       │              DATABASE BOUNDARY                │
       │═══════════════════════════════════════════════│
       │
       ▼
┌──────────────┐
│ PostgreSQL   │
│ Database     │
└──────┬───────┘
       │ Executes SQL
       │ Returns rows
       │
       │═══════════════════════════════════════════════│
       │              DATABASE BOUNDARY                │
       │═══════════════════════════════════════════════│
       │
       ▼
┌──────────────┐
│ Prisma       │
│ Maps to      │
│ TypeScript   │
└──────┬───────┘
       │ TypeScript objects
       ▼
┌──────────────┐
│ Service      │
│ Returns data │
└──────┬───────┘
       │ Data object
       ▼
┌──────────────┐
│ Controller   │
│ JSON response│
└──────┬───────┘
       │ HTTP Response
       │ Status: 200
       │ Body: JSON
       │
       │═══════════════════════════════════════════════│
       │              NETWORK BOUNDARY                 │
       │═══════════════════════════════════════════════│
       │
       ▼
CLIENT SIDE (Frontend)
──────────────────────
┌──────────────┐
│ API Client   │
│ Parses JSON  │
└──────┬───────┘
       │ Data object
       ▼
┌──────────────┐
│ Loader       │
│ Transforms   │
│ data         │
└──────┬───────┘
       │ Transformed data
       ▼
┌──────────────┐
│ Component    │
│ useLoaderData│
└──────┬───────┘
       │ Props
       ▼
┌──────────────┐
│ UI Component │
│ Renders      │
└──────────────┘
```

---

## 🔑 Key Components

### Frontend Components
```
apps/frontend/
├── app/
│   ├── routes/
│   │   ├── login.tsx                    # Login form & action
│   │   ├── _protected.tsx               # Protected route wrapper
│   │   └── _protected.dashboard.tsx     # Dashboard loader
│   ├── lib/
│   │   ├── api.ts                       # API client
│   │   ├── auth.server.ts               # Auth utilities
│   │   └── session.server.ts            # Session management
│   └── components/
│       └── Dashboard.tsx                # UI component
```

### Backend Components
```
apps/backend/
├── src/
│   ├── auth/
│   │   ├── auth.controller.ts           # Auth endpoints
│   │   ├── auth.service.ts              # Auth logic
│   │   ├── guards/
│   │   │   └── jwt-auth.guard.ts        # Auth guard
│   │   └── strategies/
│   │       └── jwt.strategy.ts          # JWT validation
│   ├── events/
│   │   ├── events.controller.ts         # Events endpoints
│   │   └── events.service.ts            # Events logic
│   └── prisma/
│       └── prisma.service.ts            # Database service
```

### Database Components
```
packages/database/
├── src/
│   └── client.ts                        # Prisma client
└── prisma/
    └── schema.prisma                    # Database schema
```

---

## 📊 Data Flow Summary

1. **User Action** → Form submission or navigation
2. **Remix Route** → Server-side loader/action
3. **API Client** → HTTP request with auth token
4. **Backend Controller** → Receives request
5. **Auth Guard** → Validates JWT token
6. **Service** → Business logic & query building
7. **Prisma** → Database query execution
8. **PostgreSQL** → SQL execution & data return
9. **Prisma** → Maps to TypeScript objects
10. **Service** → Returns data
11. **Controller** → JSON response
12. **API Client** → Parses response
13. **Loader** → Transforms data
14. **Component** → Receives props
15. **UI** → Renders data

---

**Use this diagram to trace any request from start to finish! 🎯**

