export default {
  plugins: {
    "@tailwindcss/postcss": {
      // Tailwind CSS v4 configuration
      // The CSS warnings about :where() syntax are harmless
      // These are valid modern CSS features
    },
  },
};

