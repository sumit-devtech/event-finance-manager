/**
 * ExpenseTracker - Re-export for backward compatibility
 * 
 * This file maintains backward compatibility with existing imports.
 * New code should import from ~/components/expenses instead.
 */

export { ExpenseTracker } from "./expenses";
